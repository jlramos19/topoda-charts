{
  "name": "topoda-charts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/rendered-html.test.mjs",
    "deploy": "firebase deploy --only hosting --project topoda"
  }
}
